// Literally everything in here is placeholder so searchmenu.java stops freaking out
public class ViewedList {

    public void addMovie(Movie movie) {
        System.out.println(movie.getTitle() + " added to viewed list.");
    }
}